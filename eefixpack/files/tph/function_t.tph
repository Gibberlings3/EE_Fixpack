// 2.7 changed t() to getUiString(); add alias for older UI mods
APPEND "util.lua" "
-- compatibility alias for UI mods
if getUiString ~= nil and t == nil then
  t = getUiString
end"
  UNLESS "function t(stringName)"   // don't add a t() alias if there's one present (e.g. 2.6)
